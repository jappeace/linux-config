# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
bindsym --release $mod+Ctrl+e exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 jappieklooster@hotmail.com"
bindsym --release $mod+Ctrl+o exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 jappie.klooster@daisee.com"
bindsym --release $mod+Ctrl+p exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 0031522\ 471280"
bindsym --release $mod+Ctrl+m exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 00316\ 40166188"
bindsym --release $mod+Ctrl+i exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 00628\ 7763810853"
bindsym --release $mod+Ctrl+a exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 0061\ 452476854"
bindsym --release $mod+Ctrl+s exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Kerkdijk\ 2"
bindsym --release $mod+Ctrl+c exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 The\ Netherlands"
bindsym --release $mod+Ctrl+d exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Drenthe"
bindsym --release $mod+Ctrl+l exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Ansen"
bindsym --release $mod+Ctrl+z exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 7964KB"
bindsym --release $mod+Ctrl+n exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Jappie\ Klooster"
bindsym --release $mod+Ctrl+1 exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Jappie"
bindsym --release $mod+Ctrl+2 exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 Klooster"
bindsym --release $mod+Ctrl+w exec --no-startup-id "xdotool keyup ctrl keyup alt type --delay 5 https://jappieklooster.nl"

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec "rofi -threads 8 -combi-modi window,ssh,run -show combi -modi combi"
bindsym $mod+s exec "rofi -threads 8 -combi-modi window,ssh,run -show ssh -modi combi"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

set $left h
set $right l
set $down j
set $up k

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking # nobody uses stack
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# naming of workspaces
set $w1 1-bit
set $wf1 1f-@
set $w2 2-sh
set $wf2 2f
set $w3 3-file
set $wf3 3f
set $w4 4-ide
set $wf4 4f
set $w5 5
set $wf5 5f
set $w6 6
set $wf6 6f
set $w7 7
set $wf7 7f
set $w8 8
set $wf8 8f
set $w9 9-chrm
set $wf9 9f
set $w10 10-mon
set $wf10 10f

# switch to workspace
bindsym $mod+1 workspace $w1
bindsym $mod+F1 workspace $wf1
bindsym $mod+2 workspace $w2
bindsym $mod+F2 workspace $wf2
bindsym $mod+3 workspace $w3
bindsym $mod+F3 workspace $wf3
bindsym $mod+4 workspace $w4
bindsym $mod+F4 workspace $wf4
bindsym $mod+5 workspace $w5
bindsym $mod+F5 workspace $wf5
bindsym $mod+6 workspace $w6
bindsym $mod+F6 workspace $wf6
bindsym $mod+7 workspace $w7
bindsym $mod+F7 workspace $wf7
bindsym $mod+8 workspace $w8
bindsym $mod+F8 workspace $wf8
bindsym $mod+9 workspace $w9
bindsym $mod+F9 workspace $wf9
bindsym $mod+0 workspace $w10
bindsym $mod+F10 workspace $wf10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+F1 move container to workspace $wf1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+F2 move container to workspace $wf2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+F3 move container to workspace $wf3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+F4 move container to workspace $wf4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+F5 move container to workspace $wf5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+F6 move container to workspace $wf6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+F7 move container to workspace $wf7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+F8 move container to workspace $wf8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+F9 move container to workspace $wf9
bindsym $mod+Shift+0 move container to workspace $w10
bindsym $mod+Shift+F10 move container to workspace $wf10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Should I close this session sir?' -b 'Yes..' 'i3-msg exit'"

# screenshot
bindsym --release $mod+c exec gnome-screenshot -c

bindsym --release $mod+Shift+x exec gnome-screenshot -c -a
# resize window (you can also use the mouse for that)
mode "resize" {
		# These bindings trigger as soon as you enter the resize mode

		# Pressing left will shrink the window’s width.
		# Pressing right will grow the window’s width.
		# Pressing up will shrink the window’s height.
		# Pressing down will grow the window’s height.
		bindsym $left resize shrink width 10 px or 10 ppt
		bindsym $down resize grow height 10 px or 10 ppt
		bindsym $up resize shrink height 10 px or 10 ppt
		bindsym $right resize grow width 10 px or 10 ppt

		# back to normal: Enter or Escape
		bindsym Return mode "default"
		bindsym Escape mode "default"
}
bindsym --release $mod+i exec emacsclient -c

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
# status_command i3status
# colors {
#   separator #666666
#   background #000000
#   statusline #ffffff
#   focused_workspace #551a8b #551a8b #ffffff
#   active_workspace #333333 #5f676a #ffffff
#   inactive_workspace #000000 #000000 #888888
#   urgent_workspace #2f343a #900000 #ffffff
# }
#}
exec --no-startup-id xfce4-panel --disable-wm-check

assign [class="Steam"] $w1
assign [class="transmission" instance="transmission-qt" title="Transmission"] $w1
assign [class="Deluge"] $w1
assign [class="Chromium"] $w8
assign [class="ksysguard"] $w10
assign [class="keepassxc"] $wf8

client.background \#000000
# class					border	backgr. text	indicator

#bind brightness ctl
bindsym XF86MonBrightnessDown exec brightness-
bindsym XF86MonBrightnessUp exec brightness+
bindsym $mod+Shift+s exec sudo sleep-memory
bindsym $mod+Shift+z exec --no-startup-id /home/jappie/bin/monitor
bindsym $mod+Shift+a exec --no-startup-id /home/jappie/bin/monitor-2

#bindsym $mod+Shift+i exec sudo reboot

#bind invert colors
bindsym $mod+n exec xcalib -i -a

#bind browser to b, use new window otherwise it'l complain
bindsym $mod+b exec firefox -new-window

#block hosts if I'm slacking to much
bindsym $mod+Shift+b exec sudo hostblock -b

#invert colors
#not necisary with kde
#exec xcalib -i -a
#startup some applications
#exec i3-msg 'workspace $w1;exec dropbox; exec dbus-launch nm-applet &'
exec dbus-launch nm-applet &
exec fcitx

# exec i3-msg 'workspace $wf1; exec thunderbird' #... disabled cuase its addicting
exec i3-msg 'workspace $w9; exec firefox'
exec i3-msg 'workspace $w10;exec ksysguard'
# exec i3-msg 'workspace $w1; exec deluge'
exec i3-msg 'workspace $wf8; exec keepassxc'
# exec i3-msg 'workspace $w2;exec konsole'
# exec i3-msg 'workspace $w3;exec dolphin'
#exec i3-msg 'workspace $w1;exec steam'
exec sudo brightness 500

client.focused #3b1261 #3b1261 #ffffff #662b9c
client.focused_inactive #333333 #5f676a #ffffff #484e50
client.unfocused #222222 #222222 #888888 #292d2e
client.urgent #2f343a #900000 #ffffff #900000

exec --no-startup-id while true; do feh --bg-scale `files=(Afbeeldingen/wallpapers/*); printf "%s\n" "${files[RANDOM % ${#files[@]}]}"`; sleep 600; done
